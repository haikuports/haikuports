From f4ca6447c3f178c98605593d61418365ce161bcf Mon Sep 17 00:00:00 2001
From: Ken Mays <kmays2000@gmail.com>
Date: Wed, 1 Jul 2026 01:12:06 -0700
Subject: Fix ENOMEM warnings


diff --git a/src/util/os_file.c b/src/util/os_file.c
index 7d99383..14a86d5 100644
--- a/src/util/os_file.c
+++ b/src/util/os_file.c
@@ -156,7 +156,7 @@ os_read_file(const char *filename, size_t *size)
    char *buf = malloc(len);
    if (!buf) {
       close(fd);
-      errno = -ENOMEM;
+      errno = ENOMEM;
       return NULL;
    }
 
@@ -167,7 +167,7 @@ os_read_file(const char *filename, size_t *size)
       if (!newbuf) {
          free(buf);
          close(fd);
-         errno = -ENOMEM;
+         errno = ENOMEM;
          return NULL;
       }
 
@@ -187,7 +187,7 @@ os_read_file(const char *filename, size_t *size)
    char *newbuf = realloc(buf, len);
    if (!newbuf) {
       free(buf);
-      errno = -ENOMEM;
+      errno = ENOMEM;
       return NULL;
    }
    buf = newbuf;
-- 
2.54.0


From 981bfd90a9c77ccb00efc9626b5a05fc22b362b2 Mon Sep 17 00:00:00 2001
From: Augustin Cavalier <waddlesplash@gmail.com>
Date: Fri, 2 Jan 2026 09:42:13 -0600
Subject: renderdoc: Add Haiku platform support


diff --git a/include/renderdoc_app.h b/include/renderdoc_app.h
index b2d10c5..debd0c7 100644
--- a/include/renderdoc_app.h
+++ b/include/renderdoc_app.h
@@ -35,7 +35,7 @@
 
 #if defined(WIN32) || defined(__WIN32__) || defined(_WIN32) || defined(_MSC_VER)
 #define RENDERDOC_CC __cdecl
-#elif defined(__linux__)
+#elif defined(__linux__) || defined(__HAIKU__)
 #define RENDERDOC_CC
 #elif defined(__APPLE__)
 #define RENDERDOC_CC
-- 
2.54.0

