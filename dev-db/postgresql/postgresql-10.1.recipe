SUMMARY="A powerful object-relational database system"
DESCRIPTION="PostgreSQL has more than 15 years of active development and a \
proven architecture that has earned it a strong reputation for reliability, \
data integrity, and correctness.

* It runs on all major operating systems, including Linux, UNIX (AIX, BSD, \
HP-UX, SGI IRIX, Mac OS X, Solaris, Tru64), and Windows.
* It is fully ACID compliant, has full support for foreign keys, joins, \
views, triggers, and stored procedures (in multiple languages).
* It includes most SQL:2008 data types, including INTEGER, NUMERIC, BOOLEAN, \
CHAR, VARCHAR, DATE, INTERVAL, and TIMESTAMP.
* It supports storage of binary large objects, including pictures, sounds, or \
video.
* It has native programming interfaces for C/C++, Java, .Net, Perl, Python, \
Ruby, Tcl, ODBC, among others, and exceptional documentation."
HOMEPAGE="https://www.postgresql.org/"
COPYRIGHT="1996-2017 PostgreSQL Global Development Group"
LICENSE="PostgreSQL"
REVISION="1"
SOURCE_URI="http://ftp.postgresql.org/pub/source/v$portVersion/postgresql-$portVersion.tar.bz2"
CHECKSUM_SHA256="3ccb4e25fe7a7ea6308dea103cac202963e6b746697366d72ec2900449a5e713"
SOURCE_DIR="postgresql-$portVersion"
PATCHES="postgresql-$portVersion.patchset"

ARCHITECTURES="x86_gcc2 x86 x86_64"
SECONDARY_ARCHITECTURES="x86"

PROVIDES="
	postgresql$secondaryArchSuffix = $portVersion compat >= 10
	cmd:clusterdb$secondaryArchSuffix = $portVersion compat >= 10
	cmd:createdb$secondaryArchSuffix = $portVersion compat >= 10
	cmd:createlang$secondaryArchSuffix = $portVersion compat >= 10
	cmd:createuser$secondaryArchSuffix = $portVersion compat >= 10
	cmd:dropdb$secondaryArchSuffix = $portVersion compat >= 10
	cmd:droplang$secondaryArchSuffix = $portVersion compat >= 10
	cmd:dropuser$secondaryArchSuffix = $portVersion compat >= 10
	cmd:ecpg$secondaryArchSuffix = $portVersion compat >= 10
	cmd:initdb$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_archivecleanup$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_basebackup$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_config$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_controldata$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_ctl$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_dump$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_dumpall$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_isready$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_receivewal$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_receivexlog$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_recvlogical$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_resetwal$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_resetxlog$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_restore$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_rewind$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_test_fsync$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_test_timing$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_upgrade$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pg_waldump$secondaryArchSuffix = $portVersion compat >= 10
	cmd:pgbench$secondaryArchSuffix = $portVersion compat >= 10
	cmd:postgres$secondaryArchSuffix = $portVersion compat >= 10
	cmd:postmaster$secondaryArchSuffix = $portVersion compat >= 10
	cmd:psql$secondaryArchSuffix = $portVersion compat >= 10
	cmd:reindexdb$secondaryArchSuffix = $portVersion compat >= 10
	cmd:vacuumdb$secondaryArchSuffix = $portVersion compat >= 10
	lib:libecpg$secondaryArchSuffix = $portVersion compat >= 10
	lib:libecpg_compat$secondaryArchSuffix = $portVersion compat >= 10
	lib:libpgtypes$secondaryArchSuffix = $portVersion compat >= 10
	lib:libpq$secondaryArchSuffix = $portVersion compat >= 10
	"
REQUIRES="
	haiku$secondaryArchSuffix
	lib:libicuuc$secondaryArchSuffix
	lib:libicui18n$secondaryArchSuffix
	lib:libicudata$secondaryArchSuffix
	lib:libreadline$secondaryArchSuffix
	lib:libxml2$secondaryArchSuffix
	lib:libxslt$secondaryArchSuffix
	lib:libz$secondaryArchSuffix
	"

PROVIDES_devel="
	postgresql${secondaryArchSuffix}_devel = $portVersion
	devel:libpgport$secondaryArchSuffix = $portVersion
	devel:libpgcommon$secondaryArchSuffix = $portVersion
	devel:libecpg$secondaryArchSuffix = $portVersion
	devel:libpgfeutils$secondaryArchSuffix = $portVersion
	devel:libpgtypes$secondaryArchSuffix = $portVersion
	devel:libpq$secondaryArchSuffix = $portVersion
	devel:libecpg_compat$secondaryArchSuffix = $portVersion
	"
REQUIRES_devel="
	postgresql${secondaryArchSuffix} == $portVersion base
	"

BUILD_REQUIRES="
	devel:libcrypto$secondaryArchSuffix
	devel:libicuuc$secondaryArchSuffix
	devel:libicui18n$secondaryArchSuffix
	devel:libicudata$secondaryArchSuffix
	devel:libldap$secondaryArchSuffix
	devel:libpam$secondaryArchSuffix
	devel:libreadline$secondaryArchSuffix
	devel:libssl$secondaryArchSuffix
	devel:libxml2$secondaryArchSuffix
	devel:libxslt$secondaryArchSuffix
	devel:libz$secondaryArchSuffix
	"
BUILD_PREREQUIRES="
	haiku${secondaryArchSuffix}_devel
	cmd:aclocal
	cmd:autoconf
	cmd:automake
	cmd:autoheader
	cmd:awk
	cmd:bison
	cmd:flex
	cmd:gcc$secondaryArchSuffix
	cmd:ld$secondaryArchSuffix
	cmd:libtoolize$secondaryArchSuffix
	cmd:make
	cmd:m4
	cmd:pkg_config$secondaryArchSuffix
	"

BUILD()
{
	libtoolize --force --copy --install
	autoreconf -i

	touch missing

	CFLAGS=-D_BSD_SOURCE runConfigure ./configure \
		--with-icu \
		--with-ldap \
		--with-libxml \
		--with-libxslt \
		--with-openssl \
		--with-pam
	make $jobArgs
}

INSTALL()
{
	make install

	prepareInstalledDevelLibs libpgport \
		libpgcommon \
		libecpg \
		libpgtypes \
		libpq \
		libecpg_compat \
		libpgfeutils
	fixPkgconfig

	mv $libDir/postgresql/pgxs $developLibDir/postgresql/

	packageEntries devel \
		$developDir
}

TEST()
{

	LIBRARY_PATH="$sourceDir/tmp_install/packages/postgresql-$portVersion-$REVISION/.self/lib${LIBRARY_PATH:+:$LIBRARY_PATH}"
	make check
}
