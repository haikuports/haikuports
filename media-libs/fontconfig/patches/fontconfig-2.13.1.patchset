From 289e73e717ede225300fbc07fda0a05b14d1abc9 Mon Sep 17 00:00:00 2001
From: begasus <begasus@gmail.com>
Date: Mon, 27 May 2019 18:54:54 +0200
Subject: gcc2 fix


diff --git a/src/fccfg.c b/src/fccfg.c
index d7c48e8..e8bbd50 100644
--- a/src/fccfg.c
+++ b/src/fccfg.c
@@ -2228,6 +2228,7 @@ FcConfigRealFilename (FcConfig		*config,
     const FcChar8 *sysroot = FcConfigGetSysRoot (config);
     FcChar8 *n = FcConfigFilename (url);
     FcChar8 *nn = NULL;
+    FcChar8 *path = FcStrBuildFilename;
 
     if (n)
     {
@@ -2251,7 +2252,7 @@ FcConfigRealFilename (FcConfig		*config,
 		if (!dirname)
 		    return NULL;
 
-		FcChar8 *path = FcStrBuildFilename (dirname, buf, NULL);
+		*path = FcStrBuildFilename (dirname, buf, NULL);
 		FcStrFree (dirname);
 		if (!path)
 		    return NULL;
-- 
2.21.0


From ed9761935e5900664df85832c5e90245a71a6082 Mon Sep 17 00:00:00 2001
From: Jerome Duval <jerome.duval@gmail.com>
Date: Tue, 10 Jun 2014 16:31:39 +0000
Subject: Disable some tests


diff --git a/test/Makefile.am b/test/Makefile.am
index 79bcede..5f186a3 100644
--- a/test/Makefile.am
+++ b/test/Makefile.am
@@ -52,7 +52,7 @@ TESTS += test-bz131804
 noinst_PROGRAMS = $(check_PROGRAMS)
 
 if !OS_WIN32
-check_PROGRAMS += test-migration
+check_PROGRAMS += 
 test_migration_LDADD = $(top_builddir)/src/libfontconfig.la
 endif
 
@@ -79,7 +79,7 @@ test_hash_CFLAGS = -I$(top_builddir) -I$(top_builddir)/src $(UUID_CFLAGS)
 test_hash_LDADD = $(UUID_LIBS)
 TESTS += test-hash
 
-check_PROGRAMS += test-bz106632
+check_PROGRAMS += 
 test_bz106632_CFLAGS =					\
 	-I$(top_builddir)				\
 	-I$(top_builddir)/src				\
-- 
2.21.0

