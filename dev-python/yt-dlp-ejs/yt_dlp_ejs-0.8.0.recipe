SUMMARY="External JavaScript for yt-dlp supporting many runtimes"
DESCRIPTION="This package is required when using yt-dlp to download videos from YouTube."
HOMEPAGE="https://pypi.org/project/yt-dlp-ejs/"
COPYRIGHT="<n/a>"
LICENSE="Unlicense
	MIT
	ISC"
REVISION="1"
SOURCE_URI="https://files.pythonhosted.org/packages/py3/${portName:0:1}/$portName/$portName-$portVersion-py3-none-any.whl#noarchive"
CHECKSUM_SHA256="79300e5fca7f937a1eeede11f0456862c1b41107ce1d726871e0207424f4bdb4"

ARCHITECTURES="any"

PYTHON_VERSIONS=(3.14)

for pythonVersion in ${PYTHON_VERSIONS[@]}; do
	pythonPackage=python${pythonVersion//.}

	# Allows using dot in sub-package names (eg: "_python3.14" vs "_python314"):
	eval "PACKAGE_NAME_$pythonPackage=${portName}_python$pythonVersion"

	eval "PROVIDES_$pythonPackage=\"
		${portName}_python$pythonVersion = $portVersion
		\""

	eval "REQUIRES_$pythonPackage=\"
		haiku
		cmd:python$pythonVersion
		\""

	BUILD_REQUIRES+="
		installer_$pythonPackage
		"
	BUILD_PREREQUIRES+="
		cmd:python$pythonVersion
		"
done

INSTALL()
{
	for pythonVersion in ${PYTHON_VERSIONS[@]}; do
		pythonPackage=python${pythonVersion//.}

		python=python$pythonVersion
		$python -m installer -p $prefix $portName-$portVersion-*-none-any.whl

		packageEntries $pythonPackage \
			$prefix/lib/$python
	done
}
